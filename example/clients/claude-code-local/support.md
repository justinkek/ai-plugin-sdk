Every hook runs: the rule is printed when a session starts, a long reply is
noted and the note is replayed on the next prompt. Settings are kept in
`~/.example-plugin` and outlive the session.

Not verified on a real session. It is an example, and the SDK builds it to
prove the build rather than to be installed.
