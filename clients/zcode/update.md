Update it under Settings, Plugin Management, where it was added. The bundled
command line cannot install or update a plugin, so the only way in is the
window.

Your settings are in `{{home}}` and no update touches them.
