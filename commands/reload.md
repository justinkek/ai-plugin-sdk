---
description: Print what {{name}} puts into a session at its start, when nothing printed it
---

Invoke the skill named `{{name}}:reload`, or `reload` if this copy was
installed without a plugin, and follow it.
